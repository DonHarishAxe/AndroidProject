<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="ctf.task.task3.DisplayActivity">

    <TextView
        android:id="@+id/checkListTitle"
        android:paddingBottom="10dp"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/background_material_dark"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <ListView
        android:id="@+id/display_list"
        android:layout_width="wrap_content"
        android:layout_height="250dp"
        android:layout_below="@+id/button345">

        </ListView>

    <ListView
        android:layout_marginTop="20dp"
        android:layout_below="@id/display_list"
        android:id="@+id/display_done_list"
        android:layout_width="wrap_content"
        android:layout_height="250dp">

        </ListView>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add item"
        android:id="@+id/button345"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/display_list"
        android:layout_alignEnd="@+id/display_list" />
</RelativeLayout>
